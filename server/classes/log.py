class Log:
    def __init__(self, vazao_de_bancada_a,wps,temperatura_oleo,pressao_embreagem,id_teste,tps,time,velo_rte,amortecedor_te,forca_g_long,amortecedor_de,tensao_bateria,velo_rtd,pressao_oleo,temperatura_ar,temperatura_motor,pressao_diferencial_combustivel,sonda_geral,pressao_freio,rpm,marcha,velo_rfe,id_logs,link,descricao,forca_g_lateral,id_piloto):
        self.vazao_de_bancada_a = float(vazao_de_bancada_a)
        self.wps = float(wps)
        self.temperatura_oleo = float(temperatura_oleo)
        self.pressao_embreagem = float(pressao_embreagem)
        self.id_teste = int(id_teste)
        self.tps = float(tps)
        self.time = time
        self.velo_rte = float(velo_rte)
        self.amortecedor_te = float(amortecedor_te)
        self.forca_g_long = float(forca_g_long)
        self.amortecedor_de = float(amortecedor_de)
        self.tensao_bateria = float(tensao_bateria)
        self.velo_rtd = float(velo_rtd)
        self.pressao_oleo = float(pressao_oleo)
        self.temperatura_ar = float(temperatura_ar)
        self.temperatura_motor = float(temperatura_motor)
        self.pressao_diferencial_combustivel = float(pressao_diferencial_combustivel)
        self.sonda_geral = float(sonda_geral)
        self.pressao_freio = float(pressao_freio)
        self.rpm = float(rpm)
        self.marcha = int(marcha)
        self.velo_rfe = float(velo_rfe)
        self.id_logs = id_logs
        self.link = str(link)
        self.descricao = str(descricao)
        self.forca_g_lateral = float(forca_g_lateral)
        self.id_piloto = int(id_piloto)

    def modifica(self, vazao_de_bancada_a,wps,temperatura_oleo,pressao_embreagem,id_teste,tps,time,velo_rte,amortecedor_te,forca_g_long,amortecedor_de,tensao_bateria,velo_rtd,pressao_oleo,temperatura_ar,temperatura_motor,pressao_diferencial_combustivel,sonda_geral,pressao_freio,rpm,marcha,velo_rfe,id_logs,link,descricao,forca_g_lateral,id_piloto):
        """Modifica todos os atributos do objeto."""
        self.vazao_de_bancada_a = float(vazao_de_bancada_a)
        self.wps = float(wps)
        self.temperatura_oleo = float(temperatura_oleo)
        self.pressao_embreagem = float(pressao_embreagem)
        self.id_teste = int(id_teste)
        self.tps = float(tps)
        self.time = time
        self.velo_rte = float(velo_rte)
        self.amortecedor_te = float(amortecedor_te)
        self.forca_g_long = float(forca_g_long)
        self.amortecedor_de = float(amortecedor_de)
        self.tensao_bateria = float(tensao_bateria)
        self.velo_rtd = float(velo_rtd)
        self.pressao_oleo = float(pressao_oleo)
        self.temperatura_ar = float(temperatura_ar)
        self.temperatura_motor = float(temperatura_motor)
        self.pressao_diferencial_combustivel = float(pressao_diferencial_combustivel)
        self.sonda_geral = float(sonda_geral)
        self.pressao_freio = float(pressao_freio)
        self.rpm = float(rpm)
        self.marcha = int(marcha)
        self.velo_rfe = float(velo_rfe)
        self.id_logs = id_logs
        self.link = str(link)
        self.descricao = str(descricao)
        self.forca_g_lateral = float(forca_g_lateral)
        self.id_piloto = int(id_piloto)
